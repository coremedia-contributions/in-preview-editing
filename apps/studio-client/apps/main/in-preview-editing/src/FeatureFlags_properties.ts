interface FeatureFlags_properties {
  IN_PREVIEW_EDITING_SHOW_METRICS: string;
}

const FeatureFlags_properties: FeatureFlags_properties = {
  IN_PREVIEW_EDITING_SHOW_METRICS: "Show metrics in In-Preview-Editing menu",
};

export default FeatureFlags_properties;
