# Section Item Editing

The plugin also allows editing section items.

## Section Item Metadata
Section items can be marked with the data attribute `data-cm-section-item` in the DOM.

## Section Item Edit actions
**Duplicate section**

The plugin adds a `+` button after each section item, which allows duplicating the section item. The duplicated section item is added after the original one.


